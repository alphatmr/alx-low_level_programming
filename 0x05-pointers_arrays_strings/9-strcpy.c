#include "main.h"

/**
 * *_strcpy - a function that copies the string pointed.
 * @dest: copy value to.
 * @src: copy value from.
 * Return: string value.
 */
char *_strcpy(char *dest, char *src)
{
	int i = 0;

	for (; src[i] != '\0'; i++)
	{
		dest[i] = src[i];
	}

	return (dust);

}
